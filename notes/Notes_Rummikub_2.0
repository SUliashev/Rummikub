To do:

to update next player button method and config

add hue around chips on the board if their combination is wrong:
    validation.slots currently show all valid slots, i need to add something so that the empty slots which dont need to be validated are not being shown 